class Animal { 
    int speak() { return 1; } 
}
class Dog extends Animal { 
    int speak() { return 2; } 
}
void main() {
    Animal a := new Animal;
    Animal d := new Dog;
    PrintInt(a.speak()); 
    PrintInt(d.speak()); 
}
